# Backend (Express)

Quick start:

Install dependencies:

```powershell
cd Backend
npm install
```

Run in dev (requires `nodemon` globally or dev dependency):

```powershell
npm run dev
```

Or start:

```powershell
npm start
```

The API exposes `POST /api/contact` and `GET /api/ping`.
